<div class="col-md-2">
</div>
<div class="col-md-10">
    <button type="submit" class="btn btn-primary">{{ trans('actions.save') }}</button>
    <a href="{{ route($route) }}" class="btn btn-default">{{ trans('actions.cancel') }}</a>
</div>
